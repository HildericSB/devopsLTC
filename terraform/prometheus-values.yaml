
grafana:
  service:
    # Change this to LoadBalancer to make it accesible and uncomment outputs
    type: "ClusterIP"

prometheus:
  service:
    # Change this to LoadBalancer to make it accesible and uncomment outputs
    type: "ClusterIP"
  additionalServiceMonitors:
    - name: "ltc-api-monitor"
      selector:
        matchLabels:
          app: "ltcapi"
      namespaceSelector:
        matchNames:
          - default
      endpoints:
      - port: "8000"
        path: "/metrics"
        interval: "15s"